apiVersion: v1
kind: Service
metadata:
  name: python-image-demo
  namespace: my-app
spec:
  selector:
    app: python-image-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP